<%# views/lotes/paso4.ejs — Instructivo de manufactura %>
<div class="step-page">
  <%- include('../partials/stepper-topbar') %>

  <div class="step-layout">
    <div class="step-main">

      <div class="step-header">
        <div class="step-header__meta">
          <span>Paso 4/9 · <%= lote.numeroLote %> · <%= lote.medicamento %> · Operario: <%= operario.nombre %></span>
          <span class="step-header__badge step-header__badge--readonly">🔒 Solo lectura · D.T.</span>
          <span class="step-header__timer">⏱ <%= tiempoTranscurrido %></span>
        </div>
        <h1 class="step-header__title">Instructivo de manufactura</h1>
      </div>

      <div class="readonly-notice">🔒 Vista de solo lectura — Director Técnico. Este paso fue completado por el Operario.</div>

      <!-- Instrucciones ejecutadas -->
      <div class="step-card">
        <p class="step-card__title">Instructivo de manufactura — registro completado</p>
        <table class="step-table">
          <colgroup>
            <col style="width:50%">
            <col style="width:25%">
            <col style="width:25%">
          </colgroup>
          <tbody>
            <% instrucciones.forEach((inst, i) => { %>
              <tr>
                <td>
                  <span class="result-ok" style="margin-right:6px">✓</span>
                  <%= inst.texto %>
                </td>
                <td></td>
                <td>
                  <% if (inst.tipo === 'check') { %>
                    <span class="result-ok">✓ <%= inst.valor %></span>
                  <% } else { %>
                    <div class="field-value" style="background:var(--nude);font-size:12px;"><%= inst.valor %></div>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
        <div style="margin-top:12px">
          <p class="obs-label">Observaciones del operario</p>
          <div class="field-value field-value--wide"><%= observaciones %></div>
        </div>
      </div>

      <!-- Checklist -->
      <div class="step-card">
        <p class="step-card__title">Checklist completado</p>
        <div class="checklist-grid">
          <% checklist.forEach(item => { %>
            <div class="check-row">
              <div class="check-box"></div>
              <span><%= item %></span>
            </div>
          <% }) %>
        </div>
      </div>

      <div class="step-card">
        <p class="obs-label">Observaciones del operario (solo lectura)</p>
        <div class="field-value field-value--wide" style="color:var(--muted)">Sin observaciones adicionales registradas en este paso.</div>
      </div>

    </div>

    <%- include('../partials/stepper-aside') %>
  </div>

  <%- include('../partials/stepper-footer') %>
</div>
